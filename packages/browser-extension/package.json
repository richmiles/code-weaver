{
  "name": "@codeweaver/browser-extension",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && tsc --noEmit --project tsconfig.tests.json && vite build",
    "preview": "vite preview",
    "check-types": "tsc --noEmit && tsc --noEmit --project tsconfig.tests.json",
    "lint": "eslint --config eslint.config.js .",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4"
  }
}
